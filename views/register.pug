extends layout

include utils

block content
    +validationErrorSummary(errors)
    form(action='/users/register' method='post')
        input(type='hidden' name='_csrf' value=csrfToken)
        +textField('Username', 'username', username)
        +textField('Email', 'email', email)
        +textField('Password', 'password', '', password)
        +textField('Confirm Password', 'confirmPassword', '', confirmPassword)
        div(class='login-register-buttons')
            button(type='submit' class='login-register-submit') Register
            a(href='/' class='login-register-cancel') Cancel
    div
    p: a(href='/users/login') Already have an account?